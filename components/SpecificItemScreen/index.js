import Details from "./Details";
import ActionButtons from "./ActionButtons";

// we export components here to be able to import it in one line
// eg: import { SISDetails } from '../components/ItemsScreen'
// we use SIS (SpecificItemScreen) at the start of the component name as identifier
export { Details as SISDetails, ActionButtons as SISActionButtons };
